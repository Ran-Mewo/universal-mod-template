plugins {
    id 'loader'
}

unimined.minecraft(sourceSets.main) {
    forge {
        loader gradle.ext.forge_loader
    }
}
runClient.javaLauncher = null
runServer.javaLauncher = null

downgradeJar.inputFile = tasks.remapJar.archiveFile
jar.finalizedBy(remapJar)
remapJar.finalizedBy(downgradeJar)